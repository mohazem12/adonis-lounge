# Adonis Lounge

Simple static website for Adonis Lounge — menu, hero, and details. Open `index.html` in a browser to view the site locally.

## Development
- No build step; static HTML/CSS
- Edit files directly in the repository (e.g., `index.html`, `styles.css`)

## Run locally
- Open `index.html` in your browser
- Or use a simple static server (Python):

```
python -m http.server 8000
```

## Pushing to GitHub
Follow the instructions in `PUSH_GUIDE.md` to connect this repository to your GitHub account and push the repo.

## License
Add your preferred license here.
